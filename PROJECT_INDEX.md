# Project Index

This index records the recovered notebook artifacts currently included in `computer-vision-reconstructions`. Descriptions are limited to information established by directory and notebook names in the available file inventory. Notebook contents must be reviewed before assigning more specific technical descriptions.

## Common execution notes

All projects are Jupyter notebooks stored under `projects/<project>/notebooks/`. No project-specific dependency manifests, dataset archives, test suites, or execution logs are listed in the recovered repository inventory.

Suggested initial procedure for any project:

1. Create an isolated Python environment.
2. Install Jupyter.
3. Inspect the notebook without executing all cells.
4. Identify imports, local paths, downloads, credentials, and dataset assumptions.
5. Obtain only authorized data.
6. Execute incrementally and record failures, missing dependencies, and environment details.

No notebook is currently recorded as validated.

---

## 1. `copy-of-118830360-proj2`

**Notebook:** `projects/copy-of-118830360-proj2/notebooks/copy-of-118830360-proj2.ipynb`

### Scope

This is a recovered notebook artifact identified by the source-style name `118830360-proj2`. The available inventory does not establish the project topic, algorithms, inputs, or expected outputs.

### Setup

Open the notebook in Jupyter after creating a local Python environment. Review imports and setup cells to determine required packages and data locations.

### Validation status

No test results, successful execution record, dependency lockfile, or runtime log is available. Validation has not been established.

### Private-data requirements

Unknown from the file inventory. Inspect file-loading and configuration cells before execution. Any original local data or paths should be treated as unavailable unless separately provided and authorized.

### Limitations

- The project name does not provide a reliable technical description.
- Dependencies and compatible Python versions are not recorded in the listed files.
- Notebook outputs may be historical and are not evidence of current reproducibility.

### Provenance

The project directory and notebook filename are retained as recovered: `copy-of-118830360-proj2`. Consult `docs/notebook-lineage.md` and `docs/ownership-and-attribution.md` for repository-level lineage and attribution records.

---

## 2. `another-shared-copy-of-shasank-patel-cmsc426-project-1`

**Notebook:** `projects/another-shared-copy-of-shasank-patel-cmsc426-project-1/notebooks/another-shared-copy-of-shasank-patel-cmsc426-project-1.ipynb`

### Scope

This is a recovered CMSC426 project notebook whose filename indicates it was an additional shared copy of a project labeled `project-1`. The file inventory does not establish the assignment subject, implementation details, or whether the notebook is complete.

### Setup

Run only after reviewing notebook cells for imports, data access, and environment assumptions. Use local configuration for paths and credentials rather than restoring private paths from historical environments.

### Validation status

No passing tests or successful notebook execution are documented. Validation is unresolved.

### Private-data requirements

Unknown from the file inventory. The shared-copy naming does not establish whether referenced data is public, course-provided, private, or absent. Review data-loading cells and confirm redistribution rights before adding any missing files.

### Limitations

- Shared-copy naming is provenance information, not a statement of authorship or ownership.
- No dependency manifest or dataset bundle is listed.
- The available metadata is insufficient to characterize the notebook's computer-vision task.

### Provenance

The recovered filename is preserved without normalization. The name includes `shasank-patel` because that text appears in the source filename; this index does not infer authorship from it. See repository attribution and lineage documentation for applicable review notes.

---

## 3. `copy-of-shasank-patel-cmsc426-project-2`

**Notebook:** `projects/copy-of-shasank-patel-cmsc426-project-2/notebooks/copy-of-shasank-patel-cmsc426-project-2.ipynb`

### Scope

This is a recovered CMSC426 notebook identified as `project-2` in its retained filename. No further technical scope can be established from the provided inventory.

### Setup

Use Jupyter in an isolated environment. Inspect imports, package versions, local file references, and any download logic before running notebook cells.

### Validation status

No validation results are provided. The notebook has not been verified to run in the current repository state.

### Private-data requirements

Unknown. Any original data directories, cloud mounts, credentials, or institution-specific paths referenced in the notebook must be replaced with authorized local configuration or documented as unavailable.

### Limitations

- The source filename is not a substitute for a project specification.
- Required data and expected results are not included in the listed inventory.
- Execution may depend on obsolete libraries or notebook-specific state.

### Provenance

The source-style directory and notebook names are retained for traceability. Text in the filename is not independently verified attribution. Refer to `docs/ownership-and-attribution.md` and `docs/notebook-lineage.md` before reuse or redistribution.

---

## 4. `ian-fuller-copy-of-cmsc426-project3`

**Notebook:** `projects/ian-fuller-copy-of-cmsc426-project3/notebooks/ian-fuller-copy-of-cmsc426-project3.ipynb`

### Scope

This recovered notebook is labeled `cmsc426-project3` in its filename and includes `ian-fuller-copy-of` as part of the retained source name. The available file list does not identify the project topic, methods, data, or outputs.

### Setup

Create an isolated environment and inspect the notebook before execution. Install only the libraries indicated by notebook imports after confirming compatible versions.

### Validation status

There is no recorded test suite, execution log, or successful run. Current reproducibility is unconfirmed.

### Private-data requirements

Unknown. Review notebook code for hard-coded filesystem paths, external storage, credentials, and data dependencies. Do not commit any recovered private inputs or credentials.

### Limitations

- Copy naming may reflect notebook distribution history rather than original implementation history.
- No declared environment or dataset is included in the available inventory.
- Outputs, if present in the notebook, may be stale or dependent on unavailable inputs.

### Provenance

The notebook remains under its recovered filename to avoid obscuring lineage. The filename alone does not establish ownership. Repository-level provenance records remain authoritative for available attribution notes.

---

## 5. `copy-of-chris-cmsc426-project4`

**Notebook:** `projects/copy-of-chris-cmsc426-project4/notebooks/copy-of-chris-cmsc426-project4.ipynb`

### Scope

This is a recovered notebook labeled `cmsc426-project4` in its retained filename. The provided file inventory does not establish the underlying computer-vision problem, implementation scope, data source, or completion state.

### Setup

Launch with Jupyter from an isolated Python environment. Before executing, inspect package imports, path handling, external resource access, and assumptions about files located outside the repository.

### Validation status

No successful execution, automated test result, or expected-output comparison is documented. Validation remains pending.

### Private-data requirements

Unknown. If the notebook references non-repository assets, determine whether they are public, course-distributed, or private before attempting reconstruction. Use public or synthetic substitute inputs where original data cannot be included.

### Limitations

- Required packages and their versions are not specified by the listed files.
- Dataset availability and redistribution status are not established.
- Historical notebook outputs do not demonstrate current correctness.

### Provenance

The `copy-of-chris-cmsc426-project4` name is retained from the recovered artifact. It is presented as filename provenance only and not as a verified statement of authorship. Consult the repository provenance documents before publication or reuse.

---

## Repository-level follow-up

Before presenting any project as reproducible, add evidence-based documentation for:

- Python and package versions;
- required public and private inputs;
- dataset licenses and access instructions;
- expected notebook outputs or acceptance checks;
- execution date, platform, and known failures;
- ownership and redistribution decisions.
